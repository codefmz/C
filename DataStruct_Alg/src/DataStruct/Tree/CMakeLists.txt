# aux_source_directory(${PROJECT_SOURCE_DIR}/src/DataStruct/Tree
#                                         tree_src_file)

#定义了一个全局的属性，属性的名字，属性的值是这个
set_property(GLOBAL PROPERTY TREE_INCLUDE_INNER ${CMAKE_CURRENT_SOURCE_DIR})

